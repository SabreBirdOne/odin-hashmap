I learned about hash tables in RPI's data structures, 
so similarly to the Linked List project, I had a good idea of how things should work.

The hash table implementation I remembered even shrinks the number of buckets 
if the number of key-value pairs go below a minimum threshold, in addition to the doubling of capacity 
in this project.

My data structure choice for each key-value pair could be better than just an array of 2 elements.
If I can at least have a custom comparison function to work with key value pairs, it would save me some work.

That reminds me, a lot of custom data structure implementations back in those RPI days that I did 
comes with comparison functions between those custom objects, just so built-in things like sort can work 
on a collection of custom objects. 

And maybe, if I had implemented ITERATORS for the buckets, I wouldn't have to keep linearly going through buckets explicitly 
like I was constantly doing for my HashMap functions, which leads to the next point.

My HashMap implementation is too tightly coupled with Linked List. Feel free to refactor this repo as an exercise for the reader~

I don't know if I should but I miss dealing with memory leaks.